{% if prototype is not defined %}{% set prototype=false %}{% endif %}
<div data-id="{% if prototype %}0{% else %}{{ note.getId }}{% endif %}" class="data-row">
    {% if isEditor %}
        {% include 'FitchTutorBundle:Profile:note_row_inner.html.twig' %}
    {% else %}
        {% include 'FitchTutorBundle:Profile:note_row_inner.readonly.html.twig' %}
    {% endif %}
</div>
