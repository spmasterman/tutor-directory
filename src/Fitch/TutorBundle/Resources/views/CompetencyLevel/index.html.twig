{% extends 'FitchFrontEndBundle::frontend.html.twig' %}

    {% block main_header -%}
    <h2>{% trans %}competencylevel.list.verb{% endtrans %}</h2>
    <em>{% trans %}competencylevel.list.tagline{% endtrans %}</em>
    {% endblock %}

{% block main_content -%}
    <div class="widget widget-table">
        <div class="widget-header">
            <h3><i class="fa fa-signal"></i> Competency Levels</h3>
            <div class="btn-group widget-header-toolbar">
                <a href="#" title="Focus" class="btn-borderless btn-focus"><i class="fa fa-eye"></i></a>
                <a href="#" title="Expand/Collapse" class="btn-borderless btn-toggle-expand"><i class="fa fa-chevron-up"></i></a>
                <a href="#" title="Remove" class="btn-borderless btn-remove"><i class="fa fa-times"></i></a>
            </div>
        </div>
        <div class="widget-content">
            <table class="records_list table table-condensed table-striped">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Created</th>
                        <th>Updated</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for competencyLevel in competencyLevels %}
                    <tr>
                        <td>{{ competencyLevel.id }}</td>
                        <td><a href="{{ path('competency_level_show', { 'id': competencyLevel.id }) }}">{{ competencyLevel.name }}</a></td>
                        <td>{% if competencyLevel.created %}{{ competencyLevel.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                        <td>{% if competencyLevel.updated %}{{ competencyLevel.updated|date('Y-m-d H:i:s') }}{% endif %}</td>
                        <td><a href="{{ path('competency_level_show', { 'id': competencyLevel.id }) }}" class="btn btn-xs btn-default">{% trans %}navigation.show{% endtrans %}</a>                        <a href="{{ path('competency_level_edit', { 'id': competencyLevel.id }) }}" class="btn btn-xs btn-default">{% trans %}navigation.edit{% endtrans %}</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
         </div>
    </div>
    <div class="record_actions well">
        <a href="{{ path('competency_level_new') }}" class="btn btn-default btn-s">
            <i class="fa fa-plus-circle"></i> {% trans %}navigation.new{% endtrans %}
        </a>
    </div>
{% endblock %}
